{
  "name": "tests-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--import=tsx cucumber-js",
    "test:headed": "PWDEBUG=1 NODE_OPTIONS=--import=tsx cucumber-js",
    "test:report": "NODE_OPTIONS=--import=tsx cucumber-js --format progress --format summary --format json:../../reports/e2e/cucumber-report.json"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "@playwright/test": "^1.48.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
